{
  "name": "agent-fleet-control",
  "version": "1.0.0",
  "private": true,
  "description": "Worktree isolation, explicit path territory, and merge receipts for coding-agent fleets.",
  "scripts": {
    "test": "bash tests/writer-isolation.sh && bash tests/merge-gates.sh"
  },
  "license": "CC0-1.0"
}
